const contractStatus = {
    terminated: 'terminated'
}

const messages = {
    noContract: "No contracts for your profile",
    noJobs: "No jobs for your query",
    conditions: "Conditions don't apply",
    depositRequire: "Add a value for deposit",
}

module.exports = {
    contractStatus,
    messages,
}